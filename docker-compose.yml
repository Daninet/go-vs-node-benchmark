version: "3.8"
services:
  postgres:
    image: postgres:12.3
    restart: unless-stopped
    volumes:
      - ./db.sql.gz:/docker-entrypoint-initdb.d/db.sql.gz
    environment:
      POSTGRES_PASSWORD: postgres

  js:
    build: ./fastify
    ports:
    - "3001:3001"

  go:
    build: ./go
    ports:
    - "3002:3002"
